import { ListWrapper } from "@/styles/mixins";
import styled from "@emotion/styled";
import React from "react";

const SecondProject = () => {
  return <ListWrapper></ListWrapper>;
};

export default SecondProject;
