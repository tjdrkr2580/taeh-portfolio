export interface homeWrapperPropsType {
  isView: boolean;
}

export interface listTitlePropsType {
  title: string;
  isStack: boolean;
  stack: string;
  href: boolean;
  link: string;
  desc: string;
}
