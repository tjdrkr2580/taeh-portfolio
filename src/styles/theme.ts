type themeTypes = {
  textColor: string;
  primaryColor: string;
  backgroundColor: string;
  strongColor: string;
  codeColor: string;
};

export const theme: themeTypes = {
  textColor: "#d9d9d9",
  primaryColor: "#a5a5a5",
  backgroundColor: "#191919",
  strongColor: "#dc5351",
  codeColor: "#161b22",
};
